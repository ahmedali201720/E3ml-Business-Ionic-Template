customElements.define('modal-content', class ModalContent extends HTMLElement {
    connectedCallback() {
        this.innerHTML = `

          <ion-header>
            <ion-toolbar>
              <ion-button class="header-button" fill="clear" onclick="dismissModal()">
                  <ion-icon class="header-icon" name="close-outline"></ion-icon>
                  About MBA
              </ion-button>
            </ion-toolbar>
          </ion-header>

          <ion-content fullscreen>

              <div class="about-container">
              
                    <ion-text class="about-text">
                        <p>
                            This text is an example of text that can be replaced in the same space. This text was generated from the Arabic text generator, where you can generate such text or many other texts in addition to increasing the number of letters generated by the application.
                            If you need a larger number of paragraphs, the Arabic text generator allows you to increase the number of paragraphs as you want, the text will not appear divided and does not contain language errors, the Arabic text generator is useful for website designers in particular, where the customer often needs to see a real picture To design the site.
                            Hence, the designer must place temporary texts on the design in order to show the customer the complete shape, the role of the Arabic text generator to save the designer the trouble of searching for alternative text that has nothing to do with the topic that the design talks about and appears to be inappropriate.
                            This text can be installed on any design without a problem and will not look like copied, unorganized, unformatted, or even incomprehensible text. Because it is still an alternative and temporary text.
                        </p>
                    </ion-text>

                    <div class="certificate-text">
                        <ion-label class="title">Completion certificate</ion-label>
                        <ion-label class="subtitle">From E3Business</ion-label>
                    </div>

                    <div class="certificate-attatchment">
                        <img src="../assets/img/certificate.png" alt="accreditation">
                    </div>

                    <div class="certificate-footer">
                        <ion-text class="certificate-footer-text">
                            <p>
                               It is an international American university and certificates can be certified through the
                               American State Department, the state, and the notary.
                            </p>
                        </ion-text>
                    </div>

                </div>
            
          </ion-content>
        `;
    }
});

let currentModal = null;
const button = document.querySelector('ion-button');
button.addEventListener('click', createModal);

async function createModal() {
    if (currentModal == null) {
        const modal = await modalController.create({
            component: 'modal-content'
        });

        await modal.present();
        currentModal = modal;
    }

}

function dismissModal() {
    if (currentModal) {
        currentModal.dismiss().then(() => { currentModal = null; });
    }
}